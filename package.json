{
  "scripts": {
    "tailwind-watch": "npx tailwindcss -i input.css -o output.css --watch",
    "tailwind-minify": "npx tailwindcss -i input.css -o output.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.1.8"
  }
}
