---
export interface Props {
	date: Date;
}

const { date } = Astro.props;

const today = new Date();
let dateString = date.toLocaleDateString('en-us', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
})
---

<time class="font-sm" datetime={date.toISOString()}>
	{ dateString }  
</time>
